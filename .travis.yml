language: python
python:
  - "3.6"

services:
  - mongodb

install: "pip install -r requirements.txt"

before_script:
  - sleep 15
  - mongo $MONGO_URI_COOKBOOK --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'
  


script:
#- SECRET_KEY="whatever"
  python3 -m unittest tests/*.py
